export default function OnboardingPage() {
  // You'll need:
  // - State for current step (useState)
  // - State for form data
  // - Functions to go next/previous step
  // - Submit handler to save to database
  
  return (
    <div>
      {/* Progress indicator showing step 1 of 5 */}
      {/* Step content based on current step */}
      {/* Navigation buttons */}
    </div>
  )
}